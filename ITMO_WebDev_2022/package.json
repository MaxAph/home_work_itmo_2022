{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode dev",
    "build": "vite build",
    "server:simple": "simple-server dist 3000",
    "server:json": "echo 'JSON Server Start' && json-server --watch db.todos.json --port 3003 --static dist -d 1000",
    "cy:open": "yarn dev & cypress open",
    "preview": "vite preview",
    "vitestui": "vitest --ui"
  },
  "devDependencies": {
    "@vitest/ui": "^0.25.1",
    "cypress": "^10.11.0",
    "prettier": "^2.7.1",
    "tailwindcss": "^3.2.4",
    "vite": "^3.2.3",
    "vite-plugin-remove-console": "^1.3.0",
    "vitest": "^0.25.1"
  },
  "dependencies": {
    "git": "^0.1.5"
  },
  "prettier": {
    "singleQuote": true
  }
}
